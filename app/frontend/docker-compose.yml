services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: biblioteca-frontend
    restart: unless-stopped

    ports:
      - "8080:80"

    networks:
      - biblioteca-frontend

networks:
  biblioteca-frontend:
    driver: bridge